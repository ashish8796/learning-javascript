{
  "name": "search",
  "version": "1.0.0",
  "description": "* Create a `package.json` file. * Create index.html * Create src folder and create index.js inside it. * Install node-sass. * Create sass folder in src and create index.scss in it. * Import index.scss in index/html * Run `npm start` command.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html"
  },
  "author": "",
  "license": "ISC"
}
